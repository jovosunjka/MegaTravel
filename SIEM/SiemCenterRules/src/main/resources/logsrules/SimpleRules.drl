package logsrules;
import com.bsep.SiemCenterRules.model.Log;
import com.bsep.SiemCenterRules.model.enums.LogLevel;

rule "Log with ERROR"
    when
        Log(logLevel == LogLevel.ERROR)
    then
        System.out.println("Alarm raised from rule: 'Log with ERROR'");
end